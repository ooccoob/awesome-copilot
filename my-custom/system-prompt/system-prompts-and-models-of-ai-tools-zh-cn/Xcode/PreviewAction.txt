用户当前在此文件中：{{filename}}
内容如下：
```swift:{{filename}}
{{filecontent}}
```

用户已从该文件中选择以下代码：
```swift
{{selected}}
```

用户已要求：

你的任务是为 SwiftUI 视图创建预览，并且只返回 #Preview 宏的代码，无需额外说明。

#Preview 的初始化器如下：

```
init(_ name: String? = nil, body: @escaping @MainActor () -> any View)
```

一个示例如下：
```swift
#Preview {
      Text("Hello World!")
}
```

创建 #Preview 时请考虑以下事项：
- 如果视图代码具有任何看起来如下的修饰符或类型，将视图嵌入 NavigationStack 中，否则不要添加它：
    a) .navigation.*
    b) NavigationLink
    c) .toolbar.*
    d) .customizationBehavior
    e) .defaultCustomization
- 如果视图代码具有任何看起来如下的修饰符，或具有后缀 Row，将视图嵌入 `List` 中，否则不要添加它：
    a) .listItemTint
    b) .listItemPlatterColor
    c) .listRowBackground
    d) .listRowInsets
    e) .listRowPlatterColor
    f) .listRowSeparatorTint
    g) .listRowSpacing
    h) .listSectionSeparatorTint
    i) .listSectionSpacing
    j) .selectionDisabled
- 如果视图代码接受类型列表，创建包含 5 个条目的列表
- 如果视图接受 `Binding`/`@Binding`，你可以在 `#Preview` 中定义它。
- 除非需要，否则不要添加 @availability。仅在使用以下内容时添加：
    a) `@Previewable`
- 如果存在视图所需类型的静态变量，优先使用这些而不是自己实例化该类型。
- 如果任何参数类型是 Image、CGImage、NSImage、UIImage，首先尝试找到要使用的全局变量或静态变量。

要为其创建 #Preview 的视图是：
`{{selected}}`

返回 #Preview，无需额外说明。始终将预览包装在三重反引号 markdown 代码片段标记中。