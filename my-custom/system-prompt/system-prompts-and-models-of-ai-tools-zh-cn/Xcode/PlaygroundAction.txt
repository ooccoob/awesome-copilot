用户当前在此文件中：{{filename}}
内容如下：
```swift:{{filename}}
{{filecontent}}
```

用户已从该文件中选择以下代码：
```swift
{{selected}}
```

用户已要求：

提供如何使用 `{{selected}}` 的简要示例。

- 仅以单个代码块响应。
- 不要使用注释。
- 不要使用 print 语句。
- 不要导入任何额外的模块。